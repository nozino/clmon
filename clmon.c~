#include <stdio.h>

int main(int argc, char **argv) {
  FILE *pFile;
  char buf[1024] = {0};

  p_file = fopen("/proc/cpuinfo", "r");

  if (p_file == NULL) {
    perror("cannot open file.");
  }

  while (feof(p_file)) {
    fgets(buf, 1024, p_file);
    printf("%s", buf);
  }
  fclose(p_file);
}
